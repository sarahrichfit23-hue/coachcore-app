# ===========================================
# Coach Core
# Environment Variables
# ===========================================

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# Database (NeonDB PostgreSQL)
# ===========================================
# Get your connection URL from https://console.neon.tech
DATABASE_URL="your_neondb_connection_url_here"

# ===========================================
# Cloudflare R2 Storage
# ===========================================
# To configure R2 for this application:
# 1. Create an R2 bucket in Cloudflare dashboard
# 2. Enable public access or configure a custom domain
# 3. Configure CORS settings (see README.md for details)
# 4. Create API tokens with R2 read/write permissions
R2_ENDPOINT=https://xxxxxxxxxxxxx.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your_access_key_id_here
R2_SECRET_ACCESS_KEY=your_secret_access_key_here
R2_BUCKET_NAME=your_bucket_name_here
# Use your R2 public domain URL (e.g., https://pub-xxxxxxxxxxxxx.r2.dev)
R2_PUBLIC_URL=https://pub-xxxxxxxxxxxxx.r2.dev
# Optional: If using a custom domain for R2, set the hostname here (e.g., cdn.yourdomain.com)
# This is used for Next.js image optimization security
R2_CUSTOM_DOMAIN=

# ===========================================
# Authentication
# ===========================================
# Generate a secure secret: openssl rand -base64 32
AUTH_SECRET=your_auth_secret_key_here

JWT_SECRET=your_auth_secret_key_here
NODE_ENV=development

# ===========================================
# SSO Configuration (Single Sign-On)
# ===========================================
# Root domain for cookie sharing (e.g., .coachcore.com for app.coachcore.com and portal.coachcore.com)
# Leave empty for localhost development (cookies will be scoped to localhost)
SSO_COOKIE_DOMAIN=
# Portal URL for redirects (e.g., https://portal.coachcore.com or http://localhost:3001)
NEXT_PUBLIC_PORTAL_URL=http://localhost:3001
# Main app URL (e.g., https://app.coachcore.com or http://localhost:3000)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# SMTP Configuration for Email Sending
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
PLATFORM_NAME=