# ===========================================
# Coach Core
# Environment Variables
# ===========================================

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# Database (Supabase PostgreSQL)
# ===========================================
# Get your connection URL from Supabase Project → Settings → Database → Connection string.
# Serverless runtime should use PgBouncer (port 6543). Prisma migrations use DIRECT_URL (port 5432).
# Ensure password is URL-encoded if it contains special characters (use `encodeURIComponent`).
# Example PgBouncer runtime URL:
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@YOUR_HOST:6543/postgres?sslmode=require&pgbouncer=true&connection_limit=1
# Example direct Postgres URL for migrations:
DIRECT_URL=postgresql://postgres:YOUR_PASSWORD@YOUR_HOST:5432/postgres?sslmode=require

# ===========================================
# Supabase Auth
# ===========================================
# Client-side and server-side Supabase auth keys for login
# Get from Supabase Project → Settings → API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# ===========================================
# Supabase Auth
# ===========================================
# Client-side and server-side Supabase auth keys for login
# Get from Supabase Project → Settings → API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# ===========================================
# Cloudflare R2 Storage
# ===========================================
# To configure R2 for this application:
# 1. Create an R2 bucket in Cloudflare dashboard
# 2. Enable public access or configure a custom domain
# 3. Configure CORS settings (see README.md for details)
# 4. Create API tokens with R2 read/write permissions
R2_ENDPOINT=https://xxxxxxxxxxxxx.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your_access_key_id_here
R2_SECRET_ACCESS_KEY=your_secret_access_key_here
R2_BUCKET_NAME=your_bucket_name_here
# Use your R2 public domain URL (e.g., https://pub-xxxxxxxxxxxxx.r2.dev)
R2_PUBLIC_URL=https://pub-xxxxxxxxxxxxx.r2.dev
# Optional: If using a custom domain for R2, set the hostname here (e.g., cdn.yourdomain.com)
# This is used for Next.js image optimization security
R2_CUSTOM_DOMAIN=

# ===========================================
# Authentication
# ===========================================
# Generate a secure secret (macOS Terminal): openssl rand -hex 32
JWT_SECRET=your_hex_secret_here

# SMTP Configuration for Email Sending
# ===========================================
# Email
# ===========================================
# Preferred: Resend
RESEND_API_KEY=
EMAIL_FROM=noreply@yourdomain.com
PLATFORM_NAME=Coach Core

# Optional: SMTP fallback if Resend fails or is not configured
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=